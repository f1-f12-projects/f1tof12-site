{
  "name": "f1tof12-site",
  "description": "F1 to F12 is a platform developed for Recruitment companies for their day to day operations.", 
  "version": "1.0.0",
  "author": "Vijay Koteswar",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "cat .env.local .env.endpoints > .env && react-scripts start",
    "build": "cat .env.production .env.endpoints > .env && react-scripts build serve -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@emotion/react": "^11.0.0",
    "@emotion/styled": "^11.0.0",
    "@mui/icons-material": "^5.18.0",
    "@mui/material": "^5.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-router-dom": "^6.30.1",
    "react-scripts": "^5.0.1",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0"
  },

  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
