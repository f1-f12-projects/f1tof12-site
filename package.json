{
  "name": "f1tof12-site",
  "description": "F1 to F12 is a platform developed for Recruitment companies for their day to day operations.",
  "version": "1.3.0",
  "author": "Vijay Koteswar",
  "license": "UNLICENSED",
  "private": true,
  "copyright": "© 2025 F1toF12. All rights reserved. Proprietary and confidential.",
  "repository": {
    "type": "git",
    "url": "PRIVATE"
  },
  "scripts": {
    "start": "(cat .env.local; echo; cat .env.endpoints) > .env && react-scripts start",
    "build": "mv .env.local .env.local.bak 2>/dev/null || true && (cat .env.production; echo; cat .env.endpoints) > .env && react-scripts build && mv .env.local.bak .env.local 2>/dev/null || true",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "@emotion/react": "^11.0.0",
    "@emotion/styled": "^11.0.0",
    "@mui/icons-material": "^5.18.0",
    "@mui/material": "^5.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-router-dom": "^6.30.1",
    "react-scripts": "^5.0.1",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0"
  },

  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
